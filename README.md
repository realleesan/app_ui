### Các bước cài đặt

1. **Clone repository:**
```bash
git clone <repository-url>
cd demo_app
```

2. **Cài đặt dependencies:**
```bash
flutter pub get
```

3. **Chạy ứng dụng:**
```bash
# Chạy trên mobile/desktop
flutter run

# Chạy trên web
flutter run -d chrome
```

## 📁 Cấu trúc Project

```
lib/
├── main.dart                 # Entry point
├── mobile_frame.dart         # Khung mobile wrapper
├── work_shift_screen.dart    # Màn hình ca làm việc chính
```
